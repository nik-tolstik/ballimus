# Telegram Mini App Migration Work Log

This file is the durable execution record for the migration. The orchestrator updates it at every handoff and phase gate. Keep entries factual, dated, and concise; do not store secrets, tokens, personally identifiable Telegram data, or raw Mini App `initData`.

## Operating rules

- Read this log before assigning a subagent or resuming a phase.
- Add an entry after each material decision, completed subagent task, verification result, commit, deployment action, or blocker.
- Include evidence such as a commit hash, command result, PR/branch reference, or test-environment result.
- Only the orchestrator edits this file directly. Implementation changes belong to subagents.

## Current status

| Field | Value |
| --- | --- |
| Overall state | Local implementation is complete and running through the development Telegram bot and ngrok. Public-card voting, the owner Mini App, and bounded Telegram avatar caching are active. Production cutover is pending explicit authorization. |
| Active phase | Phase 5 — local acceptance and production cutover handoff. |
| Next action | Reload the development Mini App and verify cached player photos in the roster and Players tab, then obtain explicit authorization before production cutover. |
| Production changes | None authorized. |
| Worktree baseline | Phase 1 scaffold/config changes are uncommitted; independent verification found no unrelated changes. |

## Established decisions

- One Telegram owner and one configured group per environment.
- NestJS backend on Railway with PostgreSQL, Telegram webhook, and separate Cron service.
- React + Vite Mini App with TanStack Query, shadcn/ui, and Framer Motion.
- pnpm workspaces: `apps/api`, `apps/web`, `packages/domain`, `packages/db`, and `packages/api-client`.
- Nest Swagger/OpenAPI plus Orval is the only REST-contract pipeline.
- No SQLite data migration; create a clean PostgreSQL baseline migration.
- No OpenRouter or natural-language match parser.
- An owner can create `@username -> readable name` before the player votes; Telegram ID is bound later on the first update.
- Private command/admin-panel and private external-player flows are removed in the target architecture. The owner manages external participants in the Mini App.
- The visual direction in the implementation plan is approved.
- There are only local and production environments. Local uses the test Telegram group and local services; production uses Railway and the original group. No Railway staging/test environment will be created.

## Phase board

| Phase | Status | Required outcome | Verification / exit criterion | Owner action needed |
| --- | --- | --- | --- | --- |
| 0. Scope and preparation | Complete | Confirm target scope, approved visual direction, local/production model, and migration boundary. | Decisions are recorded; no legacy data export is needed. | None. |
| 1. Workspace and quality gates | Complete | pnpm monorepo, strict TypeScript, local PostgreSQL test path, root scripts. | Independent verifier passed frozen install, lint, tests, typecheck, build, contract check, Compose validation, and helper safety checks. | None. |
| 2. Domain and PostgreSQL | Complete | Async PostgreSQL persistence, baseline migration, outbox, idempotency, late-bound player aliases. | Fresh PostgreSQL integration suite passes 10/10, including stable external-participant updates, threshold transitions, reconciliation reset, concurrency, aliases, idempotency, outbox, and job leases. | None. |
| 3. API, webhook, and jobs | Complete | Nest API, Mini App guard, Telegram webhook, Cron jobs, Swagger. | Current API suite passes 12 files/79 tests. Threshold/lifecycle notifications, post-commit bounded outbox dispatch, explicit attach/retry reconciliation, configurable init-data expiry, and complete response contracts are covered. No runtime polling or production changes. | None for local implementation. |
| 4. Contracts and Mini App | Complete | Orval client and owner-facing React/Vite app. | Reproducible OpenAPI/Orval generation passes with typed response and path models. All planned match lifecycle, grouped roster, vote correction/removal, external participant, player/alias, history, preview, conflict, and expired-auth flows are implemented; web suite passes 12/12. Browser/Chromium remains unavailable for rendered screenshots. | None for local implementation. |
| 5. Local acceptance and cutover | Ready for authorized cutover | Local acceptance, legacy cleanup, then production Railway deployment. | Current local acceptance passes frozen install, 17 workspace test files/114 tests, lint, typecheck, production build, contract drift, migration drift, script syntax, and `git diff --check`. CI and a correctly signed local init-data fixture generator are present. Production and manual live-Telegram acceptance are not recorded. | Explicit authorization for production deployment, webhook, Mini App URL, and BotFather changes. |

## History

| Date | Phase | Event | Evidence / result | Next action |
| --- | --- | --- | --- | --- |
| 2026-07-29 | Planning | Created the migration handoff plan and orchestration package. | `97fdf36` documents the Mini App plan; this documentation commit adds `prompt.md` and the work log. | Confirm Phase 0 scope and start delegated Phase 1 work. |
| 2026-07-29 | Legacy baseline | Preserved the existing vote-removal feature before the migration. | `b23ccbc`; `pnpm test` passed 137 tests and `pnpm lint` passed. | Keep this commit as the pre-migration baseline. |
| 2026-07-29 | Phase 0 | Owner approved the visual direction and selected local plus production only. | Local uses the test group; production uses the original group and Railway. No Railway staging environment. | Begin delegated Phase 1 work. |
| 2026-07-29 | Phase 1 | Delegated root workspace/tooling, five package shells, and a local PostgreSQL path; added strict configs and root quality-gate scripts. | Independent verifier: `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm test` (23 files, 137 tests), `pnpm typecheck`, `pnpm build`, and `pnpm api:contracts:check` all passed. Compose config, helper syntax, safe-reset refusal, `.env.local.example` tracking, and `git diff --check` passed. No production changes; no commit created. | Start Phase 2 domain and PostgreSQL persistence assignments. |
| 2026-07-29 | Phase 2 | Started bounded implementation handoffs for pure domain rules, PostgreSQL schema/migrations, repositories, and integration tests. | File ownership is disjoint; local and production resources remain separate. | Integrate delegated slices and assign an independent Phase 2 verifier. |
| 2026-07-29 | Phase 2 | Completed delegated domain, PostgreSQL foundation, and async repository slices. | Domain package: 12 tests plus lint/typecheck/build passed. DB foundation: baseline schema/migration, repositories, transactions, outbox, and job claims; lint/typecheck/build/db:check passed. No migration or production connection was run. | Add and run fresh-PostgreSQL integration/concurrency tests. |
| 2026-07-29 | Phase 2 | Closed the domain and PostgreSQL persistence gate after independent verification. | Local PostgreSQL healthy; fresh-schema DB suite 9/9 passed; domain 12/12 passed; root lint/test/typecheck/build/contracts passed; no `better-sqlite3` imports in `apps/**` or `packages/**`; no production changes. | Start delegated Phase 3 API, webhook, and jobs work. |
| 2026-07-29 | Phase 3 | Delegated the Nest API platform, REST operations, Telegram webhook/effects, outbox dispatch, and bounded jobs slices. | Feature files are present under disjoint API scopes; app-module integration exposed a Telegram controller DI failure before the phase gate. No production changes. | Repair Telegram DI and wire weather execution into the short-run jobs path. |
| 2026-07-29 | Phase 3 | Closed the API, webhook, and jobs gate after independent verification. | API suite: 11 files/71 tests passed; OpenAPI generation succeeded to a temporary file with 20 public `/v1` paths, excluded health/webhook/cron paths, and `telegramMiniApp` security on 23 operations; API lint/typecheck/build passed; local PostgreSQL `jobs:run` exited 0 with zero claims and zero failures; runtime scans found no polling/timers/drop-pending behavior; `git diff --check` passed. No production changes. | Start delegated Phase 4 OpenAPI/Orval and Mini App work. |
| 2026-07-29 | Phase 4 | Closed the OpenAPI/Orval and Mini App gate after independent verification. | `pnpm install --frozen-lockfile`, root lint/test/typecheck/build, and `pnpm api:contracts:check` passed. Generated bootstrap, matches, and players hooks are non-void; health/webhook/cron are excluded from the public contract; `apps/web/src` has no raw HTTP calls or handwritten REST DTOs. Rendered browser QA remains unavailable because Browser/Playwright/Chromium is not installed; source/component gates pass. No production changes. | Run local acceptance and inventory the final legacy cleanup. |
| 2026-07-29 | Phase 5 | Started local acceptance and completed a read-only legacy inventory. | Inventory identified the root `src/`, root SQLite/Drizzle files, parser/OpenRouter surface, legacy tests, scripts, dependencies, and docs for one final cleanup; target `apps/**` and `packages/**` implementation must be preserved. The ignored local `data/database.db` is not to be deleted without explicit cutover approval. | Run the local acceptance matrix before assigning the final cleanup change. |
| 2026-07-29 | Phase 5 | Completed the final legacy cleanup and local acceptance gate. | Removed the root SQLite/parser/OpenRouter/long-polling/private-command source, legacy tests, root migrations, and obsolete dependencies/scripts; preserved `data/database.db`. Updated the environment template and maintained docs. Final verification passed `pnpm install --frozen-lockfile`, lint, 102 tests, typecheck, build, contract drift, fresh local PostgreSQL migration, 9/9 DB tests, and local `jobs:run`; static scans found no maintained-code legacy remnants. Markdown links and `git diff --check` passed. Live Telegram manual testing was not run because no non-production bot/tunnel was configured. No production changes. | Request explicit production cutover authorization. |
| 2026-07-29 | Completion audit | Closed the remaining implementation-plan gaps found during a requirement-by-requirement audit. | Added all missing owner Mini App operations and history detail; exact Swagger response/path contracts; configurable auth expiry and expired-session UX; correctly signed local auth fixture; threshold/lifecycle notifications; bounded post-commit outbox dispatch; stable external-participant updates; recoverable attach/retry publication reconciliation; CI and updated operator docs. Final verification passed 17 files/114 tests, lint, typecheck, build, migration drift, contract drift, script syntax, and `git diff --check`. Browser QA could not run because no browser automation runtime is installed; live Telegram was not attempted without non-production bot/tunnel configuration. No production changes. | Request explicit production cutover authorization; optionally run the documented local live-Telegram matrix first. |
| 2026-07-30 | Live Telegram debugging | Fixed explicit local `tsx` runtime injection for outbox/weather Telegram effects and made publication-repair deduplication unique per idempotent operation. | The original failure occurred before any Telegram send. API suite passes 12 files/78 tests; API typecheck and lint pass. A safe retry reached Telegram, which returned `Bad Request: chat not found`; read-only Bot API checks confirm the development bot cannot access the configured test group. Browser plugin and Playwright are unavailable, so no rendered automation was run. | Add the development bot to the configured test group, then repeat the safe publication retry and verify the stored Telegram message reference. |
| 2026-07-30 | Live Telegram debugging | Published both local public cards after the development bot was added, then fixed vote callbacks failing before persistence. | Telegram reported webhook HTTP 500 responses while `telegram_updates` and `votes` remained empty. A direct reproduction showed grammY rejected manual webhook dispatch with `Bot not initialized`; `TelegramBotService` now initializes grammY with a bounded deadline before `handleUpdate`. A safe malformed-callback readiness check returns HTTP 204 without a database mutation. API suite passes 12 files/79 tests; API typecheck, lint, and `git diff --check` pass. | Click one current card button and verify the real update, vote row, callback acknowledgement, and durable card refresh. |
| 2026-07-30 | Mini App polish | Removed the standalone Telegram card preview panel from the matches screen while preserving the preview action inside the match editor. | Web suite passes 2 files/12 tests; web typecheck, lint, and `git diff --check` pass. The live Vite module returns HTTP 200 and no longer contains the removed panel title. Browser plugin and Playwright remain unavailable for screenshot validation. | Reload the running Mini App to inspect the updated matches screen. |
| 2026-07-30 | Match UX redesign | Rebuilt the matches flow from an inline list/detail stack into a list that opens a dedicated match screen with Overview, Roster, and Settings sections. | The selected mobile design direction is implemented with preserved API actions, focused lifecycle guidance, roster management, match parameters, Telegram controls, and back navigation. Web suite passes 2 files/13 tests; typecheck, lint, production build, live Vite module checks, and `git diff --check` pass. Browser-rendered design QA is recorded as blocked in `design-qa.md` because the browser capture bridge is unavailable. | Inspect the live Mini App in Telegram and report any visual or interaction refinements. |
| 2026-07-30 | Match UX refinement | Simplified lifecycle controls and improved roster readability after live owner feedback. | External source labels no longer duplicate the Russian `От` prefix; manual card refresh and settings lifecycle blocks were removed; cancellation now uses a required-reason bottom sheet from Overview; roster votes show initials avatars. Domain 12/12, web 14/14, API 79/79, and PostgreSQL 10/10 tests pass; lint, typecheck, and production build pass. | Reload the live Mini App and verify the cancellation sheet and roster layout in Telegram. |
| 2026-07-30 | Roster drag-and-drop | Replaced the three per-player vote-state buttons with accessible drag-and-drop between roster groups. | Mouse, delayed touch, and keyboard sensors use the existing vote-correction mutation with an optimistic move and error rollback; droppable groups and the drag overlay expose clear active states. Web suite passes 2 files/15 tests; root lint, typecheck, production build, and `git diff --check` pass. Browser plugin and local Playwright remain unavailable, so rendered pointer/touch automation is not recorded. | Reload the live Mini App and verify one touch drag between roster groups in Telegram. |
| 2026-07-30 | Notification polish | Removed all success toast notifications from owner mutations. | Successful operations now refresh the affected dashboard data silently; only `toast.error` remains in the maintained web source. Web suite passes 2 files/15 tests; web lint, typecheck, and `git diff --check` pass. | Continue live Telegram acceptance without success-notification noise. |
| 2026-07-30 | Preview removal | Removed the remaining Telegram-card preview UI from the match editor. | The editor now has one primary save action; preview state, callback props, client hook usage, and rendered preview output were removed from the web app while the server contract remains unchanged. Web suite passes 2 files/15 tests; web lint and typecheck pass. | Continue live Telegram acceptance with the simplified editor. |
| 2026-07-30 | External-player roster UX | Moved external participants into the Going roster group and replaced the inline form with a bottom sheet. | The Going header has a plus icon beside a combined people-count badge; external records use a `?` avatar, source label, and `×N` badge while preserving edit/remove actions. Source and positive-integer quantity validation are covered; web suite passes 2 files/15 tests, lint, typecheck, and `git diff --check`. | Verify the add/edit sheet and combined Going count in Telegram. |
| 2026-07-30 | Player pseudonym UX | Reduced the Players tab to readable-name management for users who already appeared through Telegram. | Only confirmed players render; selecting one opens a bottom sheet for a single understandable pseudonym. Pre-creating players and creating/updating/removing username aliases were removed from web state and mutation wiring. Web suite passes 2 files/16 tests; lint, typecheck, and `git diff --check`. | Verify that a voter with an unclear Telegram name can be renamed from the Mini App. |
| 2026-07-30 | Match overview hierarchy | Reordered the match Overview and surfaced publication state in the match header. | Summary now precedes Next action, the public-card status appears before the date, and the redundant Telegram navigation row was removed from Summary. Static rendering covers the hierarchy; web suite passes 2 files/16 tests, lint, and typecheck. | Reload the live Mini App and verify the updated hierarchy in Telegram. |
| 2026-07-30 | Match settings cleanup | Removed the standalone Telegram card from Match settings. | Settings now contain match parameters and conditional reconciliation only; the component receives the narrow action surface it uses. A focused static-rendering regression test was added; web suite passes 2 files/17 tests, lint, and typecheck. | Reload the live Mini App and verify the simplified Settings tab in Telegram. |
| 2026-07-30 | Cancellation reason UX | Replaced free-form-only cancellation input with a structured reason Select. | Bad weather and insufficient players submit canonical text immediately; Other conditionally reveals a required custom field. Static rendering and reason-resolution tests pass in the 2-file/18-test web suite; lint, typecheck, and production build pass. | Verify the Select and conditional Other field in Telegram. |
| 2026-07-30 | Confirmation notification copy | Replaced the technical confirmed-match message with a player-facing Telegram announcement. | The notification now shows a bold roster-ready heading, localized weekday/date/time, venue, field cost, total Going count, and clickable mentions for Going voters only; Maybe and Not going voters are excluded. Domain 12/12 and API 80/80 tests pass; lint, typecheck, and build pass. The local API and ngrok webhook were restarted successfully. | Confirm a future active match and inspect the resulting Chat-topic notification. |
| 2026-07-30 | Header cleanup | Removed the redundant Owner badge from the Mini App header. | The team identity and theme toggle remain, while the role badge and its unused icon import were removed. Static rendering covers the absence; web suite passes 2 files/18 tests, lint, typecheck, and production build. | Reload the live Mini App and inspect the simplified header in Telegram. |
| 2026-07-30 | Environment branding | Replaced the generic Mini App header identity with Ballimus branding. | Development renders Ballimus Dev with the warm logo; production renders Ballimus with the cool logo. The supplied 1254px PNGs remain untouched while 192px WebP assets reduce each runtime payload to about 4 KB. The document title uses the same static environment selection. Web suite passes 2 files/18 tests, lint, typecheck, production build, and live asset checks. | Reload the live Mini App and inspect the Ballimus Dev header in Telegram. |
| 2026-07-30 | Player avatars | Added bounded Telegram profile-photo caching and avatar rendering. | An applied vote refreshes a stale photo best-effort without affecting vote durability; the smallest JPEG/PNG/WebP is limited to 256 KiB, stored in PostgreSQL, and returned only as an authenticated `data:` URL. Initials and external-player `?` fallbacks remain. API 82/82, web 18/18, PostgreSQL 11/11, and domain 12/12 tests pass; lint, typecheck, build, contract drift, live migration, real Bot API download, authenticated REST output, webhook, and ngrok health checks pass. The one existing confirmed local player was backfilled successfully. | Reload the live Mini App and inspect the avatar in the roster and Players tab. |
| 2026-07-30 | Individual external players | Replaced aggregated external-participant cards with one editable record per person. | Creating source `N` with quantity 3 now atomically creates `N #1`, `N #2`, and `N #3`; cards have no quantity badge and each name/delete action is independent. The migration expands legacy quantity rows and enforces quantity 1. API 82/82, web 18/18, PostgreSQL 11/11, lint, typecheck, build, contract drift, live migration, API/web health, webhook, and ngrok checks pass. | Reload the live Mini App and verify add-three plus single-player rename in a match roster. |
| 2026-07-30 | Native form submission | Made Enter submit every save form in the Mini App. | Match create/edit, external-player add/edit, player pseudonym, cancellation, and Telegram-card reconciliation now use native `form`/`onSubmit` semantics with explicit submit and secondary button types. Web 19/19, lint, typecheck, production build, live API/web health, webhook, and ngrok checks pass. | Reload the live Mini App and verify Enter while renaming an external player. |
| 2026-07-30 | Drag-drop finish polish | Removed the end-of-drop card flicker in the roster. | The dnd overlay remains the only animated element during landing; the underlying card no longer performs a second opacity transition when dnd-kit restores it. Web 19/19, lint, typecheck, production build, live module, and ngrok checks pass. Browser plugin and Playwright are unavailable, so the final touch gesture remains a Telegram device check. | Reload the live Mini App and verify one touch drop between roster groups. |
| 2026-07-30 | Semantic drop zones | Added status-colored feedback while dragging a roster player. | The active Going zone uses the success token, Maybe uses a new light/dark warning token, and Not going uses the destructive token across border, background, ring, heading, and empty prompt. Web 19/19, lint, typecheck, production build, live module/style, and ngrok checks pass. | Verify all three hover states with a touch drag in Telegram. |
| 2026-07-30 | Form typography | Unified the form-control type scale and height across the Mini App. | Input, Select, date/time triggers, and the venue ToggleGroup now use 16px text and 40px height on mobile, with the same responsive compact scale on wider viewports. The change lives in shared shadcn controls and explicit form-size variants rather than per-form typography overrides. Web 19/19, lint, typecheck, production build, live module, and ngrok checks pass. | Reload the live Mini App and inspect match, cancellation, external-player, and pseudonym forms. |

## Open decisions and blockers

- Local live-Telegram testing uses the configured non-production bot, test group, and temporary HTTPS tunnels. Never record their secrets in this file or point the production bot at a local tunnel.
- Production deployment, database migration, webhook registration, Mini App URL configuration, and BotFather menu changes require explicit owner authorization; none have been performed.
